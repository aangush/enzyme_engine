name: enzyme_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10.19
  - biopython
  - python-dotenv
  - tqdm
  - pip
  - pip:
      - tqdm==4.67.1
      - requests
      - aiohttp

